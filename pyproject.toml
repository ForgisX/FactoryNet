[project]
name = "xelerit-base"
version = "0.1.0"
description = "A base repository for Xelerit projects."
authors = [{name = "Xelerit"}]
requires-python = ">=3.8"

[tool.setuptools]
packages = ["core", "manual_processing"] 